
/**
 * This class calculates the surface area and volumn 
 * of a cube of the provided side length.
 *
 * @author  (your name)
 * @version (today's date)
 */
public class Cube
{
    public static int surfaceArea(int length)
    {
        return length * length * 6;   // replace with your calculation
    }
    
    public static int volume(int length)
    {
        return length * length * length;   // replace with your calculation
    }
}